vigil:
  jwt:
    secret: test-secret-key-minimum-32-chars-long
    access-ttl: 15m
    refresh-ttl: 7d
  filter:
    public-paths:
      - /public/**
      - /health
      - /auth/**
  tenant:
    enabled: true
  protection:
    max-attempts: 3

